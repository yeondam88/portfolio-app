import React from "react";

const UserImage = props => <div className="user-image" />;

const ImageContainer = props => (
  <div className="image-container">
    <UserImage />
  </div>
);

export default ImageContainer;
